# Documentation

- Storybook (interactive playground) — built to ./storybook-static via `npm run build-storybook`
- API Reference — generated to ./api via `npm run typedoc`
- Component Docs — generated to ./vX.Y via `npm run docs:generate`
- Search Index — docs/search-index.json
